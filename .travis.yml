language: node

sudo: required

services:
  - docker

env:
 - COMPOSE_VERSION=1.5.2

before_install:
  # Docker Compose Install
  - curl -L https://github.com/docker/compose/releases/download/${COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  # Download and install Docker libs
  - curl -L https://github.com/Ortus-Solutions/docker-buildfiles/archive/master.zip > docker.zip
  - unzip docker.zip -d workbench
  - mv workbench/docker-buildfiles-master workbench/docker
  # CommandBox Keys
  - sudo apt-key adv --keyserver keys.gnupg.net --recv 6DA70622
  - sudo echo "deb http://downloads.ortussolutions.com/debs/noarch /" | sudo tee -a
    /etc/apt/sources.list.d/commandbox.list

jobs:
  include:
    - stage: test
      before_script: docker-compose build test
      script: docker-compose run test --exit-code-from test

    - stage: build
      before_script: docker-compose build test
      script: docker-compose run build --exit-code-from test

    - stage: version
      before_script: docker-compose build version
      script: docker-compose run version --exit-code-from test
      branches:
        only:
          - master
